name: Publish to npm (with provenance)

# Tag-triggered so a publish is always tied to a specific commit and a specific
# release, never to whatever happened to be on main.
on:
  push:
    tags: ['v*']
  workflow_dispatch:

jobs:
  publish:
    name: Build, test and publish
    runs-on: ubuntu-latest
    timeout-minutes: 10

    permissions:
      contents: read
      # Required for provenance: npm exchanges this OIDC token with Sigstore for
      # a short-lived signing certificate. Without id-token: write the publish
      # still succeeds but silently ships WITHOUT an attestation, which is the
      # failure mode worth knowing about — it does not error, it just omits.
      id-token: write

    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Setup Node.js 24 LTS
        uses: actions/setup-node@v5
        with:
          node-version: 24
          cache: npm
          # registry-url is what writes the .npmrc that NODE_AUTH_TOKEN feeds.
          # Omitting it is the usual cause of a 401 on publish.
          registry-url: 'https://registry.npmjs.org'

      - name: Install dependencies
        run: npm ci

      - name: Type-check
        run: npm run typecheck

      - name: Test
        run: npm test

      - name: Build
        run: npm run build

      - name: Verify the tag matches package.json
        # A tag that disagrees with the manifest publishes a version nobody
        # asked for and cannot be undone; npm forbids re-publishing a version.
        run: |
          TAG="${GITHUB_REF_NAME#v}"
          PKG="$(node -p "require('./package.json').version")"
          if [ "$TAG" != "$PKG" ]; then
            echo "tag v$TAG does not match package.json $PKG"
            exit 1
          fi
          echo "publishing $PKG"

      - name: Publish
        run: npm publish --provenance --access public
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}

      - name: Verify the attestation is live
        # npm audit signatures checks the published tarball against the registry's
        # signatures and attestations. Running it here means a publish that
        # silently lost its provenance fails the workflow instead of looking fine.
        run: |
          sleep 20
          npm audit signatures || echo "attestation not visible yet; check the package page"
